#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
	int n;
	string input;

	cin >> n;

	vector<string> arr(n, 0);

	for (int i = 0; i < n; i++) {
		cin >> arr[n];

	}
}